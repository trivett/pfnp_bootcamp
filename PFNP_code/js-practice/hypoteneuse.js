// write a function that returns the length of a hypoteneuse, given the lengths of the other two sides of a right triangle.
// you know the drill


function hypoteneuseOf(a,b){
  let response = Math.sqrt(a**2 + b**2);
  return response;
}
